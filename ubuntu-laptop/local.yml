- hosts: localhost
  become: true

  roles:
    - { role: general, tags: ['general'] }
    - { role: vcode, tags: ['vcode'] }
    - { role: docker, tags: ['docker'] }
    - { role: virtualbox, tags: ['virtualbox'] }
    - { role: spotify, tags: ['spotify'] }
